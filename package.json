{
  "name": "helion-axis",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "HELION AXIS — a launch film for a car that only exists in light.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host"
  },
  "dependencies": {
    "gsap": "^3.13.0",
    "lenis": "^1.1.14"
  },
  "devDependencies": {
    "vite": "^5.4.11"
  }
}
